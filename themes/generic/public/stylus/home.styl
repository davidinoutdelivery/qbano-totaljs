/*------------------------------------*\
    $HOME
\*------------------------------------*/   
/*Validation Ege's*/
.ege_validation
  fixed()
  width 100%
  height 100vh
  overflow-x hidden
  overflow-y auto
  top 0 
  left 0
  background-color backgroundColorBlack 
  background-position center
  background-repeat no-repeat
  background-image url('../images/error-bg.jpg')
  background-size cover
  z-index 99999999
  text-align center
  box()
  padding 20px
  flex()  
  trans(.4s)
  &.close
    opacity 0
    visibility hidden
  .logo
    inline()
    img
      margin 0 auto
      height 140px
      block()
      +below(642px)
        height 105px
  h1
    color white
    font(30px)
    fontb()
    margin 20px 0 10px
    +below(642px)
      font(28px)
  h2
    color white
    font(22px)
    fontr()
    margin 5px 0 15px
    +below(642px)
      font(18px)
  .form-user-validate
    text-align center
    block()
    margin 40px 0 30px
    +below(642px)
      margin 28px 0 30px
    .form-item
      inline()
      margin 0 15px 20px
      +below(642px)
        margin 0 10px 20px
      label
        block()
        text-align center
        margin 0 0 5px
        fontb()
        font(16px)
        color white
        span
          color red
      input
        width 65px
        height 55px
        background white
        border none
        color primaryColorText
        font(33px)
        padding 10px
        text-align center
        +below(642px)
          width 38px
          height 35px
          font(18px)
      &.form-item-anio
        input
          width 150px
          +below(642px)
            width 95px
    .btnA
      margin 22px auto 0
  


/*Slider*/
body 
  .bg_slider
    relative()
    over()
    margin 0 0 40px 0
    //min-height 568px
    //max-height 870px
    padding 82px 0 0
    &:after
      clear()
    &:before
      width 100%
      height 540px
      absolute()
      z-index 1
      content ''
      top 0
      background url('../images/bg_header.jpg') center no-repeat
      background-size cover
      +below(642px)
        height 175px
    +below(800px)
      padding 65px 0 0
    +below(642px)
      min-height 25vh
      max-height 25vh
      margin 0 0 3px 0
    .mask
      background rgba(black, .55)   
  .carrusel_slider 
    width 100%  
    max-height 60%
    margin 0
    trans(.4s)
    over()
    block()  
    z-index 2
    relative() 
    &:after
      clear()
    +below(642px)
      max-height 38vh
    li
      over()
      relative()
      width 100%
      height 574px
      cursor pointer
      //min-height 100vh
      +below(642px) 
        height 24vh   
      .imgslide
        width 100%
        height 100%
        background-color white
        background-size cover
        background-repeat no-repeat
        background-position top center
        over()
        border-radius 10px
        // -webkit-animation scaleslide 35s infinite
        // background-attachment fixed
        absolute()
        trans(.4s)
        border none
        +below(642px)
          height 24vh
          padding 
        //   background-size 100% 100%
      img
        width 100%
        none()   
        border-radius 10px
      .mask
        background rgba(black, .5)
    //Controls
    .owl-dots
      absolute()
      z-index 2
      bottom 11px
      right 12px
      margin-top 0
      .owl-dot
        span
          margin 5px 5px
          background rgba(white, .5)
        &.active,
        &:hover
          span
            background white




/*------------- Filter Home -------------*/
body
  .bg_slider
    .navOption
      inline()
      margin 10px auto 65px
      trans(.3s)
      // width 90%
      // max-width 390px
      background-color white
      border-radius 500px
      border 5px solid white
      &:after
        clear()
      +below(642px)
        margin 10px auto 47px
      label
        background-color transparent
        box-shadow none
        background-color transparent
        color white
        text-shadow inherit
        line-height 16px
        font(12px)
        fontl()
        min-width 120px
        height 38px
        padding-left 0px
        letter-spacing .5px
        padding-right 0px
        +below(642px)
          min-width 110px
        +below(375px)
          min-width 103px
        +below(372px)
          min-width 100px
        +below(362px)
          min-width 99px
        +below(360px)
          min-width 90px
        +below(320px)
          min-width 86px
        i
          block()
          box()
          padding 6px 0 18px
          font(25px)
          color #bbbbbb
        &:before
        &:after
          none()
        &.filtroTwo
          i
            padding 7px 0 18px
            font(23px)
      input:focus[type="radio"],
      [type="radio"]:not(:checked), 
      [type="radio"]:checked
        box-shadow none
        border none
      input:checked[type="radio"] + label,
      [type="radio"]:checked + label
        box-shadow none
        border none
        // color primaryColor
        i
          color black
    .contTabNav
      none()
      over()
      relative()
      .alertGeo
        absolute()
        bottom 100%
        left 0
        width 100%
        height 100%
        background green
        z-index 2
        color white
        flex()
        text-align center
        opacity 0
        trans(.4s)
        &.open
          opacity 1
          bottom 0
      .loader
        border 6px solid rgba(white, .43)
        border-top 6px solid white
        border-radius 50%
        width 13px
        height 13px
        margin 0 12px
        animation spin 2s linear infinite
      @keyframes spin 
        0% 
          transform rotate(0deg)
        100% 
          transform rotate(360deg)
body 
  .address-takeout-panel
    margin auto
    max-width 900px
    padding 0
    width 100%
    absolute()
    bottom 100px
    left 0
    right 0
    z-index 3
    text-align center
    +below(642px)
      width 90%
      bottom 72px
    h3
      color white
      font(11px)
      box()
      block()
      padding 0 0 23px
    .formBB
      font(0px)
      input
      select
        vertical-align top
        display inline-block
        width auto
        font(14px)
        fontl()
        text-align left
        border-right 1px solid grayBorder
        margin-bottom 10px
        height 54px       
        line-height 54px 
        &.small
          width 7%
          text-align center
          +below(642px)
            width 29.2%
          +below(428px)
            width 29.2%
          +below(412px)
            width 29%
          +below(400px)
            width 29.2%
          +below(375px)
            width 28.7%
          +below(360px)
            width 28.5%
          +below(320px)          
            width 27.9%
        &.large
          width 23.5%        
          +below(642px)
            width 100%
            block()
        &.block
          width 100%
          block()
        &.medium
          width 37%
          +below(642px)
            width 100%    
        &.citySelecd
          width 17.4%  
          +below(642px)                
            width 100%
      input
        padding 0px 10px
        margin-bottom 10px
        line-height 54px
        +below(642px)          
          line-height 61px
      input[type="number"]::-webkit-inner-spin-button
          -moz-appearance textfield
         -webkit-appearance none
      select
        padding 0px 35px 0px 13px
        line-height 54px
        width 18%
        +below(642px)
          width 50%  
          // line-height 56px
        +below(428px)
          width 50%
        +below(412px)
          width 50%  
        +below(320px)
          width 50%
        &.large2
          +below(642px)
            width 100%
        &.medium2
          +below(642px)
            width 50%
        &.costumer_addresses
          width 100%
          max-width 360px
      .symbol
        background-color grisbg
        color black
        vertical-align top
        display inline-block
        font(20px)
        padding 0 6px
        line-height 54px
      button.btnA
        padding 0px 36px
        height 54px
        line-height 55px
        border-radius 0px
        font(16px)
        +below(642px)
          width 100%
          block()
          height 52px
          line-height 53px
        &.block
          width 100%
          block()
.homeCober
  absolute()
  z-index 4
  bottom 15px
  left 0
  right 0
  margin auto
  color white
  fontr()
  font(12px)
  letter-spacing .5px
  sub()
  padding 12px 0
  box()
  block()
  width 40%
  text-align center
  +below(800px)
    width 100%



/*------------- Destacados Home Categories -------------*/
.feature-market
  block() 
  margin 0 0 40px   
  &::after
    clear()
  .item-feature-market
    block()
    relative()
    &::after
      clear()      
    .bg-image
      background-repeat no-repeat
      background-size cover
      background-position center
      block()
      border-radius 10px
      over()
      relative()
      width 100%
      height 180px
      trans(.4s)
      &:after
        clear()
    .txt
      relative()
      block()
      margin 10px 0
      &:after
        clear()
      h3
        block()
        fontl()
        font(16px)
        color primaryColorTitle
        text-overflow ellipsis
        white-space nowrap
        over()
        height 20px
        width 80%
      p.price
        fontb()
        font(15px)
        color primaryColorTitle
        margin 0
      .shopCart
        border-radius 50%
        right 0
        text-align center 
        color white
        top 0
        width 38px
        height 38px 
        absolute()
        z-index 2
        background primaryColor url('../images/icons/cart-icon.svg') center no-repeat
        background-size 55%
        trans(.4s)
        +below(642px)
          width 30px
          height 30px
        &:hover
          box-shadow 0 2px 10px 0 rgba(0, 0, 0, 0.29)        
    //Hover
    &:hover
      .bg-image
        box-shadow 0 2px 10px 0 rgba(0, 0, 0, 0.29)
  //Controls
  .owl-nav
    margin 0
    .owl-prev,
    .owl-next
      absolute()        
      top -75px
      bottom 0
      margin auto
      height 30px
      z-index 3
      trans(.4s)
      font(40px)
      background transparent
      color primaryColorTitle
      &.disabled
        none()
      &:hover 
        background transparent
        color primaryColorTitle
    .owl-prev
      right 100%
      margin-right 13px
    .owl-next
      left 100%
      margin-left 13px




/*------------- Video Home -------------*/
.videoHome
  min-width 100%
  min-height 100%
  width auto
  height auto
  z-index 1
  absolute()
  top 50%
  left 50%
  -webkit-transform translate(-50%,-50%)
  -moz-transform translate(-50%,-50%)
  -ms-transform translate(-50%,-50%)
  -o-transform translate(-50%,-50%)
  transform translate(-50%,-50%)



/*------------- Categorias -------------*/
.listCategori
  block()
  &:after
    clear()
.imgbgCat
  width 100%
  height 60vh
  background-position 50%
  background-repeat no-repeat
  background-size cover
  relative()
  cursor pointer
  flex()
  over()
  &:after
    clear()
  +below(542px)
    height 28vh
  &:hover
    .mask
      background rgba(black, .5)  
  h1
    fontb()
    font(23px)
    color white
    text-align center
    z-index 3
    relative()
  .mask
    background rgba(black, .3)
  


/*------------- Categorias Icons -------------*/
.list-categeory-icons
  block()
  margin 0 0 40px
  relative()
  &:after
    clear()
  li
    display inline-block
    vertical-align top
    margin 0 11px
    width 12.35%
    +below(642px)
      margin 0 4px 10px
      width 31.01%
    +below(360px)
      width 45.9%
      margin 0 6px 10px
    .panel-catg
      block()
      background-position center
      background-repeat no-repeat
      background-size cover
      trans(.4s)
      &:after
        clear()
      &:hover
        cursor pointer
        .imgBg
          transform rotateY(-5deg)  
          box-shadow 0 2px 10px 0 rgba(0, 0, 0, 0.12)
      .imgBg
        block()
        trans(.4s)
        background backgroundColorGray
        box()
        padding 14px 15px
        border-radius 10px
        &:after
          clear()
        img
          height 75px
          max-width 100%
          block()
          margin 0 auto
      h3
        block()
        fontl()
        font(15px)
        box()
        text-align center
        padding 10px 0
  



/*------------- Productos Destacados Home -------------*/
.feature-product,
.all-product
  block() 
  margin 0 0 40px   
  &::after
    clear()
  .item-feature-product
    block()
    relative()
    &::after
      clear()   
    &:hover
      cursor pointer   
    .bg-image
      background-repeat no-repeat
      background-size cover
      background-position center
      background-color backgroundColorGray
      block()
      border-radius 10px
      over()
      relative()
      width 100%
      height 165px
      trans(.4s)
      flex()      
      &:after
        clear()
      img
        box()
        padding 0px 0
        max-width 100%
        max-height 165px
        block()
        margin auto
        width auto
    .txt
      relative()
      block()
      margin 10px 0
      &:after
        clear()
      h3
        block()
        fontl()
        font(16px)
        color primaryColorTitle
        text-overflow ellipsis
        white-space nowrap
        over()
        height 20px
        width 80%
      p.price
        fontb()
        font(15px)
        color primaryColorTitle
        margin 0
      .shopCart
        border-radius 50%
        right 2px
        text-align center 
        color white
        top 0
        width 38px
        height 38px 
        absolute()
        z-index 2
        background primaryColor url('../images/icons/cart-icon.svg') center no-repeat
        background-size 55%
        trans(.4s)
        +below(642px)
          width 30px
          height 30px
        &:hover
          box-shadow 0 2px 10px 0 rgba(0, 0, 0, 0.29)
    //Hover
    &:hover
      .bg-image
        box-shadow 0 2px 10px 0 rgba(0, 0, 0, 0.29)
//Carrusel Productos
.carrusel_feature_prod      
  //Controls
  .owl-nav
    margin 0
    .owl-prev,
    .owl-next
      absolute()        
      top -75px
      bottom 0
      margin auto
      height 30px
      line-height 30px
      z-index 3
      trans(.4s)
      font(40px)
      background transparent
      color grayOne
      &.disabled
        none()
      +below(642px)
        font(32px)
      &:hover 
        background transparent
        color primaryColorTitle
    .owl-prev
      right 100%
      margin-right 13px
      +below(642px)
        margin-right 2px
    .owl-next
      left 100%
      margin-left 13px
      +below(642px)
        margin-left 2px




/*------------- Pordata Inicio -------------*/
.bg_head
  background-size cover
  background-repeat no-repeat
  background-position center
  max-width 100%
  width 100%
  height 380px
  margin 70px 0 0
  +below(800px)
    margin 64px 0 0
  +below(642px)
    background-size 100% 100%
    height 200px
    margin 64px 0 0
  +below(321px)
    height 120px
.btnshop
  absolute()
  bottom 35px
  width 100%
  right 6%
  trans(.3s)
  +below(900px)
   none()
  a:hover
    animation-name hvr-push
    animation-duration .3s
    animation-timing-function linear
    animation-iteration-count 1
  img
    height 40px
  a
    float right
  .android
    margin-right 35px
  .ios
    margin-right 14px



/*------------- Categorias --------------*/
.bg_catg
  nav
    width 100%
    height 51px
    over()
    margin 0 0 0 0
    background-color primaryColor
    trans(.5s)
    &::after
      clear both
      content ''
      block()
    .burger
      none()
    .wrapper_l
      max-width 1130px
    li
      block()
      relative()
      trans(.6s)
      text-align center
      a
        color rgba(white, .75)
        font(15px)
        fontr()
        mayus()
        block()
        relative()
        box()
        text-align center
        letter-spacing .5px
        padding 17px 24px
        trans(.5s)
        line-height 18px
        text-overflow ellipsis
        white-space nowrap
        overflow hidden
        &:hover,
        &.active
          color white
          fontb()
          trans(.5s)
          &:after
            width 100%
        &:after
          height 2px
          background white
          left 0
          right 0
          margin auto
          bottom 0px
          absolute()
          content ''
          trans(.4s)
          width 0
        +between(1050px, 1183px)
          padding 13px 15px
          

/*------------- Categorias multiple slide -------------*/
.catg_multiple
  right2()
  text-align center
  &:after
    clear()
  +below(642px)
    none()
  li
    display inline-block
  .owl-controls
    .owl-nav
      [class*='owl-']
        background url('../images/row1.png') no-repeat center center
        width 30px
        height 30px
        position absolute
        top 0
        bottom 0
        right 100%
        margin auto
        trans(.4s)
        transition all ease-in-out .3s
        border-radius 0 2px 2px 0
        &:hover
          opacity 1
        +below(642px)
          right auto
          left 0
      .owl-next
        background url('../images/row2.png') no-repeat center center
        left 100%
        right initial
        border-radius 2px 0 0 2px
        +below(642px)
          left auto
          right 0
    .owl-dots
      display none !important


/*------------- Texto bienvenida -------------*/
.bg_slide_txt
  padding 525px 0 0px
  margin 10px 0 13px
  block()
  background transparent
  &:after
    clear()  
  +below(900px)  
    padding 345px 0 26px
  +below(642px)
    padding 399px 0 26px
.txt_slider
  margin 0 0 0px 
  box()
  padding 0px
  text-align left
  h4, span
    display inline-block
    vertical-align middle
  h4
    fontb()
    font(42px)
    mayus()
    color primaryColorTitle
    box()
    padding 0 10px
    +below(642px)
      font(38px)
      mayus()
  h1
    fontr()
    font(42px)
    letter-spacing -1px
    mayus()
    color black
    border-left 1px solid primaryColor
    display inline-block
    vertical-align middle
    box()
    padding 0 10px
    +below(642px)
      font(28px)
  span
    width 78px
    height 78px
    relative()
    margin 0 0 10px
    img
      width 100%
    +below(642px)
      block()
      margin 0 auto
  .hours
    box()
    padding 6px 46px 7px 13px
    color white
    text-align left
    margin 0 0 0 10px
    font-size 12px
    vertical-align middle
    max-width 243px
    width 100%
    background black url('../images/icons/time.png') right 8px center no-repeat
    background-size 14%
    +below(642px)
      margin 10px auto 0
    span
      none()
    b,
    div
      display inline-block
      vertical-align middle
      font(16px)
    b
      margin 0 0 0 5px
      font(15px)
      



/*------------- Modal Geolocalizacion -------------*/
body 
  .tab-delivery
    block()
    margin 0 0 10px
    &:after
      clear()
    +below(642px)
      margin 26px 0 15px
    > div.padding
      +below(642px)
        padding 0
    > div > a.big
      background primaryColor url('../images/opt-1.png') right center no-repeat
      background-size auto 113%
    > div > a.domiciio
      background grayBorder url('../images/opt-2.png') right center no-repeat
      background-size auto 56%
      cursor default
      pointer-events none
      h4
        margin 5px 0 4px
    a
      block()
      background-size auto 100%
      text-align left
      box()
      padding 20px
      border-radius 6px
      color white        
      margin 0 0px 10px
      trans(.4s)
      opacity 1
      min-height 104px
      &:hover
        transform translateY(-3px)
        opacity 1
      &.active
        opacity 1
      h4
        block()
        margin 0 0 4px
        color white
        font(17px)
        fontb()
      p
        block()
        width 60%
        color white
        margin 0
  .modalGeolocalizacoin
    text-align center
    &:after
      clear()
    .backModal      
      width 32px
      height 32px
      line-height 32px
      font(15px)
      border-radius 0%
      padding 0
      absolute()
      left 6px
      top 6px
      text-align center
      color black
      cursor pointer
      block()
      trans(.3s)
      margin 4px
      z-index 4
    h2
      block()
      margin 5px 0 15px
      fontb()
      mayus()
      font(18px)
      color black
      cursor pointer
    h4
      block()
      margin 10px 0 20px
      fontb()
      color black
      span
        sub()
    .paddColumn
      padding 0 10px
      box()
      &:after
        clear()
      +below(642px)
        padding 0
    .formBB
      font(0px)     
      br
        none() 
      input
      select
        inline()
        margin 0 5px 10px
        width auto
        background-color backgroundColorGray
        border-color backgroundColorGray
        &:hover
        &:focus
          background-color backgroundColorGray
          border-color backgroundColorGray
        &.border-left
          border-radius 500px 0 0 500px
          +below(642px)
            border-radius 0
        &.border-right
          border-radius 0 500px 500px 0
          +below(642px)
            border-radius 0
        &.border
          border-radius 500px
        &.full 
          width 100%
        &.small
          width 28%
          text-align center
          +below(642px)
            width 26.8%
          +below(380px)
            width 25.8%
          +below(320px)
            width 24.7%
        &.small2
          width 28%
          +below(642px)
            width 27%
          +below(380px)
            width 26% 
          +below(360px)
            width 25%
          +below(320px)
            width 24.5%
        &.large
        &.citySelecd
          width 99%
        +below(642px)
          padding 0 9px
      select
        width 47.5%
        +below(642px)
          width 47.1% 
        +below(380px)
          width 46.5%
        +below(360px)
          width 46.5%
        +below(320px)
          width 97%
        &.costumer_addresses
          width 65%
          +below(642px)
            width 97%
      label
        font(12px)
        block()
        text-align center
      button
        margin 0 auto 20px
        &.big
          margin 20px auto 18px
      .symbol
        background-color transparent
        color black
        vertical-align top
        display inline-block
        font(14px)
        padding 0 6px
        line-height 42px  
    .homeCober
      relative()
      inline()
      margin 20px 0 0
      text-align center
      color black
      padding 0
    .btn-back
      margin-right 10px !important
      margin-left 10px !important
    .msg-map
      margin-top 5px !important
      margin-bottom 30px !important
      font(13px)
      box()
      block()
      padding 13px 5px
      border-radius 6px
      background-color rgba(grayBorder, .12)
      &:after
        clear()
      span
        font-size 21px
        display block
        margin-bottom 4px
      a
        color primaryColor
        sub()

.cont-map 
  margin 0 0 25px
  block()
  &:after
    clear()
  #map-canvas 
    width 100%
    height 210px

.div_customer_addresses
  label, p
    color white
  select
    text-align center
    width 25%

.div_go_to_login_home
  margin-bottom 20px

  p
    color white
    margin-top 20px

  a
    color white
    text-decoration-line underline
    &:hover
      color #d0ced2

.div_go_to_login_home.black-text
  p, a
    color black

.div_customer_addresses.black-text
  p, a
    color black

/* Category Styles */
.href_go_to_home
  margin-left 10px
  relative()
  top 5px
  




/*------------- Content Home Image -------------*/
.home_content
  relative()
  &:after
  &:before
    clear()
  .prod_efect1    
    background url('../images/bg_site1.png') center no-repeat
    background-size 100% 100%
    absolute()
    right -5px
    top 7%
    width 230px
    height 500px
    +below(1500px)
      right -90px
      width 180px
      height 400px
    +below(800px)
      none()
  .prod_efect2
    background url('../images/bg_site2.png') center no-repeat
    background-size 100% 100%
    absolute()
    left -5px
    top 32%
    width 240px
    height 550px
    +below(1500px)
      width 170px
      height 400px
      left -77px
    +below(800px)
      none()