/*------------------------------------*\
    $ELEMENTOS FORMULARIO
\*------------------------------------*/
label
  font(14px)
  fonts()
  color #848484
  +below(380px)
    font(13px)
  
label
  span
    color rgba(white, .85)

body 
  select
    -webkit-appearance none
    -moz-appearance none
    appearance none

button
  cursor pointer


/*--------------Input Blancos--------------------------*/
.formB
  label
    font(14px)
    fontr()
    color #848484    
    +below(380px)
      font(13px)

  select
    trans(.3s)
    -webkit-appearance none
    background-color white
    // background-image url("../images/select.png")
    background-repeat no-repeat
    background-position center right 10px
    border none
    border-radius 0px
    block()
    padding 6px 35px 7px 12px
    color black
    cursor pointer
    box()
    fontr()
    font(14px)
    width 100%
    text-align left
    margin 0px 0 10px
    -webkit-appearance none
    -moz-appearance none
    appearance none
    &:focus
      background-color white
      // background-image url("../images/select2.png")
      background-repeat no-repeat
      background-position center right 8px

  input[type="text"],
  input[type="password"],
  input[type="date"],
  input[type="datetime"],
  input[type="datetime-local"],
  input[type="month"],
  input[type="week"],
  input[type="email"],
  input[type="number"],
  input[type="search"],
  input[type="tel"],
  input[type="time"],
  input[type="url"],
  textarea
    transition all 0.2s ease-out
    -webkit-appearance none
    -moz-appearance none
    appearance none
    background-color white
    border none
    border-radius 13px
    block()
    margin 0px 0 10px 0
    padding 11px 16px
    color black
    box()
    fontr()
    font(14px)
    width 100%
    text-align center

  // input:focus[type="text"], input[type="text"]:hover,
  // input:focus[type="password"], input[type="password"]:hover,
  // input:focus[type="date"], input[type="date"]:hover,
  // input:focus[type="datetime"], input[type="datetime"]:hover,
  // input:focus[type="datetime-local"], input[type="datetime-local"]:hover,
  // input:focus[type="month"], input[type="month"]:hover,
  // input:focus[type="week"], input[type="week"]:hover,
  // input:focus[type="email"], input[type="email"]:hover,
  // input:focus[type="number"], input[type="number"]:hover,
  // input:focus[type="search"], input[type="search"]:hover,
  // input:focus[type="tel"], input[type="tel"]:hover,
  // input:focus[type="time"], input[type="time"]:hover,
  // input:focus[type="url"], input[type="url"]:hover,
  // textarea:focus, textarea:hover
  //   border 1px solid grisbtn

  textarea
    height 95px

  .ico_ubicacion
    relative()
    input[type="text"]
      padding 11px 40px 11px 15px
    &::after
      background-image url('../images/icons/icon_ubi.png')
      background-size auto 85%
      background-position center
      background-repeat no-repeat
      width 23px
      height 25px
      content ''
      absolute()
      right 11px
      top 7px
      z-index 2

  input[type="submit"]
    mayus()
    box()
    font(13px)
    border 2px solid white
    padding 9px 40px
    cursor pointer

  button
    cursor pointer

/*--------------Input Blancos Border--------------------------*/
.formBB
  label
    font(14px)
    fontr()
    block()
    color black
    margin 0 0 5px 0
    +below(643px)
      font(14px)
  select
    trans(.3s)
    -webkit-appearance none
    background-image url("../images/icons/select.png")
    background-repeat no-repeat
    background-position center right 12px
    border 1px solid backgroundColorGray
    background-color backgroundColorGray
    border-radius 100px
    block()
    padding 0px 35px 0px 12px
    color black
    cursor pointer
    box()
    fontl()
    font(14px)
    width 100%
    height 42px
    line-height 42px
    text-align left
    margin 0px 0 15px
    &:focus
      background-color white
      background-image url("../images/icons/select2.png")
      background-repeat no-repeat
      background-position center right 12px
      border 1px solid rgba(0, 0, 0, .2)
    &:hover
      border 1px solid rgba(0, 0, 0, .2)

  input[type="text"],
  input[type="password"],
  input[type="date"],
  input[type="datetime"],
  input[type="datetime-local"],
  input[type="month"],
  input[type="week"],
  input[type="email"],
  input[type="number"],
  input[type="search"],
  input[type="tel"],
  input[type="time"],
  input[type="url"],
  textarea
    transition all 0.2s ease-out
    -webkit-appearance none
    border 1px solid backgroundColorGray
    background-color backgroundColorGray
    border-radius 100px
    block()
    margin 0px 0 15px 0
    padding 0px 13px
    color black
    box()
    fontl()
    font(14px)
    width 100%
    height 42px
    line-height 42px
    &.fecha
      background-image url('../images/icons/icon_filter.png')
      background-position center right 10px
      background-repeat no-repeat

  input:focus[type="text"], input[type="text"]:hover,
  input:focus[type="password"], input[type="password"]:hover,
  input:focus[type="date"], input[type="date"]:hover,
  input:focus[type="datetime"], input[type="datetime"]:hover,
  input:focus[type="datetime-local"], input[type="datetime-local"]:hover,
  input:focus[type="month"], input[type="month"]:hover,
  input:focus[type="week"], input[type="week"]:hover,
  input:focus[type="email"], input[type="email"]:hover,
  input:focus[type="number"], input[type="number"]:hover,
  input:focus[type="search"], input[type="search"]:hover,
  input:focus[type="tel"], input[type="tel"]:hover,
  input:focus[type="time"], input[type="time"]:hover,
  input:focus[type="url"], input[type="url"]:hover,
  textarea:focus, textarea:hover
    border 1px solid rgba(black, .2)

  input:disabled[type="text"],
  input:disabled[type="password"],
  input:disabled[type="date"],
  input:disabled[type="datetime"],
  input:disabled[type="datetime-local"],
  input:disabled[type="month"],
  input:disabled[type="week"],
  input:disabled[type="email"],
  input:disabled[type="number"],
  input:disabled[type="search"],
  input:disabled[type="tel"],
  input:disabled[type="time"],
  input:disabled[type="url"],
  textarea:disabled
  select:disabled
    opacity .8
    background-color rgba(white, .7)

  textarea
    height 150px
    border-radius 14px

  input::-webkit-input-placeholder,
  textarea::-webkit-input-placeholder
    color rgba(black, .55)
  input:-moz-placeholder,
  textarea:-moz-placeholder
    color rgba(black, .55)
  input::-moz-placeholder,
  textarea::-moz-placeholder
    color rgba(black, .55)
  input:-ms-input-placeholder,
  textarea:-ms-input-placeholder
    color rgba(black, .55)

  input[type="submit"]
    mayus()
    box()
    font(13px)
    border 2px solid white
    padding 9px 40px
    cursor pointer

  button
    cursor pointer

  .checkbox
    text-align center
    width 100%
    label
      width auto
      display inline-block
      relative()
      color primaryColorText
      fontr()
      font(15px)
      &:hover
        color black
        sub()
      &::before
        border 1px solid black
        top 7px
      &::after
        background black
        top 10px

  .radiobtn
    text-align center
    width 100%
    label
      width auto
      display inline-block
      relative()
      color grisbtn
      fontr()
      font(15px)
      &:hover
        color black
        sub()
      &::before
        border 1px solid black
        top 7px
      &::after
        background black
        top 10px


/*Search Map*/
body
  .shareMap
    input[type="text"]
      max-width 599px
      right 88px
      margin auto
      background-color white
      border-radius 5px
      background-image url("../images/icons/icon_buscar.png")
      background-repeat no-repeat
      background-position center right 10px
      box-shadow 0px 3px 11px 0px rgba(50, 50, 50, 0.3)


/* Checkbox */
.checkbox
  block()
  relative()
  margin 0
  input[type=checkbox]
    visibility hidden
    none()
  label
    padding 3px 0 3px 20px
    // background url('../images/icons/check.png') left center no-repeat
    transition all 400ms
    cursor pointer
    box()
    fontr()
    font(12px)
    width 300px
    color white
    &::before
      width 11px
      height 11px
      border-radius 3px
      left 0
      top 4px
      content ''
      absolute()
      border 1px solid black
    &::after
      width 7px
      height 7px
      border-radius 50%
      left 3px
      top 7px
      content ''
      absolute()
      background black
      trans(.3s)
      transform scale(0)
    &:hover
      color black
      text-decoration none
    a
      color black
      sub()
      trans(.3s)

  input[type=checkbox]:checked + label
    &::after
      transform scale(1)
      // background url('../images/icons/checked.png') left center no-repeat


/*Radiobtn*/
.radiobtn
  block()
  relative()
  margin 0
  input[type=radio]
    visibility hidden
    none()
  label
    padding 3px 0 3px 20px
    // background url('../images/icons/check.png') left center no-repeat
    transition all 400ms
    cursor pointer
    box()
    fontr()
    font(12px)
    width 300px
    color white
    &::before
      width 11px
      height 11px
      border-radius 3px
      left 0
      top 4px
      content ''
      absolute()
      border 1px solid black
    &::after
      width 7px
      height 7px
      border-radius 50%
      left 3px
      top 7px
      content ''
      absolute()
      background black
      trans(.3s)
      transform scale(0)
    &:hover
      color white
      text-decoration none
    a
      color black
      sub()
      trans(.3s)

  input[type=radio]:checked + label
    &::after
      transform scale(1)
      // background url('../images/icons/checked.png') left center no-repeat


/*Radio Wings*/
.radiobtnWings
  display inline-block
  relative()
  margin 0
  input[type=radio]
  input[type=checkbox]
    visibility hidden
    none()
  label
    padding 53px 12px 3px
    background url('../images/icons/checkWings.png') center -106px no-repeat
    transition all 400ms
    cursor pointer
    box()
    fontr()
    font(14px)
    color primaryColorText
    margin 10px 0 5px
    &:hover
      color primaryColor
      text-decoration none
    a
      color black
      sub()
      trans(.3s)

  input[type=radio]:checked + label
  input[type=checkbox]:checked + label
    background url('../images/icons/checkWings.png') center 0px no-repeat
    color primaryColor
    span
      color primaryColor


/*Check Wings*/
.checkOpenW
  display inline-block
  relative()
  margin 0 0 10px
  input[type=checkbox],
  input[type=radio]
    visibility hidden
    none()
  label
    block()
    padding 6px 20px 7px 33px
    transition all 400ms
    cursor pointer
    box()
    fontr()
    font(18px)
    color primaryColorText
    margin 0px 0 8px
    span
      &:before
        none()
    &:hover
      color black
      text-decoration none
    &::before
      opacity 0
      visibility hidden
      z-index -2

      content ''
      position absolute
      top 2px
      left 0
      width 18px
      height 18px
      z-index 0
      border 2px solid #5a5a5a
      border-radius 1px
      margin-top 2px
      transition .2s

      box-sizing inherit
    &::after
      height 20px
      width 20px
      background-color transparent
      border 2px solid #5a5a5a
      top 8px
      z-index 0

      content ''
      left 0
      position absolute
      transition border .25s, background-color .25s, width .20s .1s, height .20s .1s, top .20s .1s, left .20s .1s
      z-index 1

      border-radius 2px

      box-sizing inherit

  input[type=checkbox]:checked + label,
  input[type=checkbox]:checked + label span,
  input[type=radio]:checked + label,
  input[type=radio]:checked + label span
    color primaryColor
    &::after
      width 20px
      height 20px
      border 2px solid primaryColor
      background-color primaryColor
      z-index 0
    &::before
      top 0
      left 1px
      width 8px
      height 13px
      border-top 2px solid transparent
      border-left 2px solid transparent
      border-right 2px solid #fff
      border-bottom 2px solid #fff
      -webkit-transform rotateZ(37deg)
      transform rotateZ(37deg)
      -webkit-transform-origin 100% 100%
      transform-origin 100% 100%

      content ''
      left 0
      position absolute
      transition border .25s, background-color .25s, width .20s .1s, height .20s .1s, top .20s .1s, left .20s .1s
      z-index 1

      left -3px
      width 12px
      height 22px
      border-top 2px solid transparent
      border-left 2px solid transparent
      border-right 2px solid white
      border-bottom 2px solid white
      -webkit-transform rotate(40deg)
      transform rotate(40deg)
      -webkit-backface-visibility hidden
      backface-visibility hidden
      -webkit-transform-origin 100% 100%
      transform-origin 100% 100%

      opacity 1
      visibility visible
      z-index 2

.bg_detalle
  .paddselectproduct
    .checkOpenW
      block()


/*Input File*/
.file
  relative()
  width 100%
  block()
  box()
  margin 0 0 20px 0
  input[type="file"]
    display none
  input[type="text"]
    height 0
    over()
    padding 0
    border none
    background-color transparent
    &.archivoAdjunto
      height auto
      overflow auto
      padding 1px 13px
      font(15px)
    &:hover
      border none
      background-color transparent
  .btnC
    block()
    width 100%
    relative()


/*Calendario*/
#root-picker-outlet
  z-index 999999
  fixed()

/*Paginador*/
.pagin
  border-top 1px solid #dddddd
  border-bottom 1px solid #dddddd
  margin 7px 0 45px 0
  block()
  relative()
  width 100%
  box()
  padding 10px 0
  text-align center
  &::after
    clear both
  a
    display inline-block
    fontr()
    font(12px)
    margin 0 4px
    box()
    trans(.3s)
    padding 7px 10px
    border 1px solid
    color #333333
    border 1px solid #dddddd
    border-radius 3px
    background white
    box-shadow 0 2px 5px 0 rgba(0,0,0,0.16)
    &.activo
      background grisbtn
      color white
    &:hover
      background grisbtn
      color white



/*Check Site*/
.checkSite
  display inline-block
  relative()
  margin 0
  input[type=checkbox],
  input[type=radio] 
    visibility hidden
    none()
  label 
    padding 6px 20px 7px 33px
    transition all 400ms
    cursor pointer
    box()
    color white
    background trans
    border 2px solid black
    border-radius 0px
    opacity .4
    trans(.3s)
    &:hover
      opacity 1
    &::before
      width 32px
      height 32px
      border 2px solid black !important
      border-radius 0px !important
      right 14px
      left auto
      top 0
      bottom 0
      margin auto
      content ''
      background transparent
      absolute()
      z-index 3      
      transition none
      +below(642px)
        width 32px
        height 32px
    &::after
      clear()
      
  input[type=checkbox]:checked + label, 
  input[type=radio]:checked + label 
    opacity 1
    &::before
      background url('../images/check.png') center no-repeat        
      background-size 55%
      right 14px
      +below(642px)
        background-size 53%
 

/*Radio Site Date*/
body 
  .checkDay
    display inline-block
    relative()
    margin 0
    width 19%
    +below(642px)
      width 30%
    input[type=checkbox],
    input[type=radio] 
      visibility hidden
      none()
    label
      padding 0px !important
      transition all 400ms
      cursor pointer
      box()
      color black
      background transparent
      border 2px solid grayBorder
      margin 0 -2px 0 0
      border-radius 0px
      text-align center
      height 70px !important
      line-height inherit !important
      display block !important
      +below(642px)
        margin -2px -2px 0 0
      &::before
      &::after
        none()
      span
        block()
        border-bottom 2px solid grayBorder
        padding 4px 4px
        font(14px)
        color black
      h3
        color black
        block()
        padding 3px 10px
        margin 0
        fontb()
        font(28px)
    input[type=checkbox]:checked + label, 
    input[type=radio]:checked + label 
      border 2px solid primaryColor
      relative()
      z-index 2
      span
        border-bottom 2px solid primaryColor
        color primaryColor
      h3
        color primaryColor



/*Radio Buttons*/
body 
  .checkBtn
    display inline-block
    relative()
    margin 0
    width 48%
    +below(400px)
      width 100%
    &.large
      width 100%
      +below(642px)
        width 48%
      label
        margin -2px -2px 0 0
        font-size .98rem !important
        +below(400px)
          font-size .98rem !important
    input[type=radio] 
      visibility hidden
      none()
    label
      padding 0px !important
      transition all 400ms
      cursor pointer
      box()
      color black
      background transparent
      border 2px solid grayBorder
      margin 0 -2px 0 0
      border-radius 0px
      text-align center
      height 40px !important
      line-height 34px !important
      display block !important
      font-size .9rem !important
      &::before
      &::after
        none()
    input[type=radio]:checked + label 
      border 2px solid primaryColor
      background-color primaryColor
      color white
      relative()
      z-index 2
      


/*Checknbox Inout*/
body [type=checkbox]+label:before, 
body [type=checkbox]:not(.filled-in)+label:after
  border: 2px solid primaryColor
  
body [type=checkbox]:checked+label:before
  border-right 2px solid primaryColor

body [type=checkbox]:checked+label:before
  border-right 2px solid primaryColor
  border-bottom 2px solid primaryColor
  

/*Radios Buttons Inout*/
body [type=radio].with-gap:checked+label:after, 
body [type=radio]:checked+label:after
  background-color primaryColor

body [type=radio].with-gap:checked+label:after,
body [type=radio].with-gap:checked+label:before,
body [type=radio]:checked+label:after,
body [type=radio]:not(:checked)+label:after, 
body [type=radio]:not(:checked)+label:before
  border 2px solid primaryColor
  



/**
  * Switch
*/
.s-switch 
  width  52px
  height  27px
  inline()
  margin  12px 1px
  right2()
  background #E5E5E5
  z-index 0
  padding 0
  appearance none
  border none
  cursor pointer
  relative()
  border-radius 16px
  input[type='checkbox']
    none()
  &:before 
    content  ''
    background #e8e8e8 !important
    absolute()
    left 1px
    top 1px
    width 52px
    height 27px
    background #FFFFFF
    z-index 1
    border-radius 16px 
  &:after 
    content ''
    height 25px
    width 25px
    border-radius 28px
    z-index 2
    background white
    absolute()
    -webkit-transition-duration 300ms
    transition-duration 300ms
    top 1px
    left 3px
    box-shadow 0 2px 5px #999999

.s-switch-on, 
.s-switch-on:before
  background  primaryColor !important
  
.s-switch-on:after
  left 26px !important
  
.s-switch-readonly, 
.s-switch-disabled
  opacity 0.8
